// Re-export the base fixture. Override or extend test/expect here if needed
// (e.g. a custom `authenticatedPage` fixture once more E2E tests exist).
export { test, expect } from "@playwright/test";
